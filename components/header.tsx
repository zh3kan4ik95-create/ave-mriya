'use client';
import { motion, AnimatePresence } from 'framer-motion';
import { ChevronDown } from 'lucide-react';
import { useState } from 'react';
import { Container, Button } from './ui';
import { appPath } from '@/lib/site-path';
export function Header() { const [open, setOpen] = useState(false); return <motion.header initial={{ y: -12, opacity: 0 }} animate={{ y: 0, opacity: 1 }} className="sticky top-0 z-30 border-b border-black/[.03] bg-white/80 backdrop-blur-xl"><Container className="flex h-20 items-center justify-between"><a href={appPath('/')} className="flex items-center gap-2 leading-none"><span className="grid h-7 w-7 place-items-center rounded-full bg-mist text-sm">🗽</span><span><span className="block text-lg font-bold tracking-[-.06em]">Ave Mriya</span><span className="mt-1 block text-[9px] uppercase tracking-[.17em] text-black/45">Road to the Dream</span></span></a><div className="flex items-center gap-2 sm:gap-3"><a href={appPath('/story')} className="hidden rounded-full px-3 py-2 text-xs font-semibold text-black/60 transition hover:bg-black/[.04] sm:block">Читати історію</a><div className="relative"><button onClick={() => setOpen(!open)} className="flex items-center gap-1 rounded-full px-2 py-2 text-xs font-semibold text-black/60 transition hover:bg-black/[.04] sm:px-3">UA <ChevronDown size={14} className={`transition ${open ? 'rotate-180' : ''}`} /></button><AnimatePresence>{open && <motion.div initial={{ opacity: 0, y: -5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }} className="absolute right-0 top-11 w-24 rounded-xl border border-black/[.07] bg-white p-1.5 shadow-soft">{['UA', 'RU', 'EN'].map(language => <button key={language} onClick={() => setOpen(false)} className="block w-full rounded-lg px-3 py-2 text-left text-xs text-black/60 transition hover:bg-mist hover:text-black">{language}</button>)}</motion.div>}</AnimatePresence></div><Button href="/offers">Стати частиною</Button></div></Container></motion.header> }
